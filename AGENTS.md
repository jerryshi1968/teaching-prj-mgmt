# 项目实现边界

- 本仓库是独立、可版本化的作品管理前端组件与领域规则仓库，不是教学网站或后端服务。
- 不读取、导入、链接或修改任何教学平台源码，也不使用跨仓库相对路径、符号链接、子模块或本地文件依赖。
- 运行时代码不得固定业务 API、访问宿主登录状态、选择作品类型或直接发起网络请求。
- 公共运行时 API 保持最小：稳定契约、纯函数核心、`ProjectOrganizer` 与 `useProjectOrganizer`。
- 数据访问、打开作品和当前目录持久化均由宿主注入；本仓库不负责鉴权、源码、运行、预览或分发。
- 修改代码时保留所有已有中英文注释及未涉及部分的原始格式。
- 交付前必须运行 `npm test`、`npm run check`、`npm run build`、`npm run pack:check` 和 `git diff --check`。
